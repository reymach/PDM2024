
let sine = new touchEnded.Synth ({
  oscillator: {
    type: 'sine'
  }
}).toDestination

let sine = new touchEnded.Synth ({
  oscillator: {
    type: 'sine'
  }
}).toDestination



function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  text('Q = Sine', 150, 150)
  
}
